// Legacy recorder UI removed. Placeholder to maintain project references.
import SwiftUI

struct AudioVisualizer: View {
    let audioMeter: AudioMeter
    let color: Color
    let isActive: Bool

    var body: some View {
        EmptyView()
    }
}

struct StaticVisualizer: View {
    let color: Color
    var body: some View { EmptyView() }
}
